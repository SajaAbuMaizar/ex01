#pragma once
#include "Set.h"

class Action
{
public:
	Action() = default;
	/*
	virtual void Uni(int command1, int command2,
		std::vector<std::string> m_commands, std::vector<int> m_num_of_params) = 0;
	virtual void Inter(int command1, int command2,
		std::vector<std::string> m_commands, std::vector<int> m_num_of_params) = 0;
	virtual void Diff(int command1, int command2,
		std::vector<std::string> m_commands, std::vector<int> m_num_of_params) = 0;
	virtual void Prod(int command1, int command2,
		std::vector<std::string> m_commands, std::vector<int> m_num_of_params) = 0;
	virtual void Comp(int command1, int command2,
		std::vector<std::string> m_commands, std::vector<int> m_num_of_params) = 0;
		*/
private:
	
};